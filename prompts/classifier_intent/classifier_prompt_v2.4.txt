You are an expert restaurant conversation analyst. Analyze this customer message to extract intents and topics.

MESSAGE TO ANALYZE:
"{message}"

## CATEGORY DEFINITIONS (STRICT - USE ONLY THESE):
- QUERY TOPICS: {docs_summaries}
- QUERY TYPES: consulting, ordering, action, complaint, confirmation, clarification, greeting, farewell, disrespecting.

## SUMMARY ORDER
"{summary_order}"

## FOCUS FORMATTING RULES (STRICT FOR ORDERING):
If query_type is ordering, the focus MUST follow the action:object format. Use commas to add parameters:
- add_item:[dish_name],qty:[n]
- change_protein:[protein_name]
- modify_qty:[number]
- set_service:[delivery|takeaway]
- specify_detail:[detail]
- replace_qty:[n],from:[item_a],to:[item_b] (Use when the user redistributes a previous quantity)
- keep_qty:[n],item:[item_name] (Use to confirm what remains after a partial change)

## ANALYSIS PROCESS:
1. SEGMENT THE MESSAGE: Split into logical segments.
   - CRITICAL RULE: Do NOT split segments that start with "no", "pero" or "mejor" if they are part of a correction of the same idea (e.g., "no, mejor con pescado" is ONE single segment).
2. ANALYZE EACH SEGMENT: Identify type, topic, and structured focus.
3. SPECIAL RULES:
   - "hola/buenas" → topic: "greeting", type: "consulting".
   - "gracias/adiós" → topic: "farewell", type: "consulting".

## THINKING PROCESS (Internal):
- STEP 0 (Context Check): Is the user redistributing or correcting a previous amount? (e.g., "one of this and the other of that"). If so, use replace_qty/keep_qty logic.
- STEP 1 (Segmentation): Prioritize semantic unity. "No, mejor..." indicates a single corrective segment.
- STEP 2 (Mapping): Ensure topic and type are in the allowed lists.
- STEP 3 (Formatting): Apply action:object,param:value structure for all ordering intents.

## CONCRETE EXAMPLES:

Example 1: "Hola, ¿tienen menú vegetariano?"
Output:
{{
"topic_details": [
{{"segment": "Hola", "query_type": "consulting", "topic": "greeting", "focus": "greeting"}},
{{"segment": "¿tienen menú vegetariano?", "query_type": "consulting", "topic": "menu", "focus": "vegetarian menu inquiry"}}
]
}}

Example 2: "Me regala dos bandeja paisa, porfa"
Output:
{{
"topic_details": [
    {{
      "segment": "Me regala dos bandeja paisa, porfa",
      "query_type": "ordering",
      "topic": "menu",
      "focus": "add_item:bandeja_paisa,qty:2"
    }}
  ]
}}

Example 3: "No, mejor uno con bocachico y el otro sí con bandeja paisa"
(Context: User previously ordered 2 bandeja paisa)
Output:
{{
"topic_details": [
    {{
     "segment": "No, mejor uno con bocachico",
      "query_type": "ordering",
      "topic": "menu",
      "focus": "replace_qty:1,from:bandeja_paisa,to:bocachico" 
    }},
    {{
      "segment": "y el otro sí con bandeja paisa",
      "query_type": "ordering",
      "topic": "menu",
      "focus": "keep_qty:1,item:bandeja_paisa"
    }}
]
}}

## CRITICAL RULES:
1. RETURN ONLY TOPIC_DETAILS: Output JSON must contain ONLY the "topic_details" field.
2. STRICT ENUM: Use ONLY allowed query_types and topics.
3. SEMANTIC SEGMENTATION: Do not orphan "no" or "mejor" particles; keep them with the correction they introduce.

## OUTPUT FORMAT (EXACT STRUCTURE):
{{
"topic_details": [
{{
"segment": "string",
"query_type": "string",
"topic": "string",
"focus": "action:object,param:value"
}}
]
}}

## NOW ANALYZE THIS MESSAGE:
"{message}"

Provide ONLY the JSON output. No other text.